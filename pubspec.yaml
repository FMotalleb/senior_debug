name: senior_debug
description: debug like a senior using `here` and `here`
version: 1.0.2
repository: https://github.com/FMotalleb/dart_senior_debug

environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  stack_trace: ^1.11.1
dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
